# dbt/stock_analytics/packages.yml

# Dependencies for dbt project.
packages:
  # Core dbt utility macros
  - package: dbt-labs/dbt_utils
    version: [">=1.1.1", "<2.0.0"]

  # Snowflake-compatible dbt_expectations
  - package: calogica/dbt_expectations
    version: [">=0.9.0", "<1.0.0"]
